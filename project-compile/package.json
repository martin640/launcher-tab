{
  "name": "project-compile",
  "version": "1.0.0",
  "scripts": {
    "prepare": "node index",
    "build": "npm run prepare && npx babel ../src --out-dir ../build --no-comments"
  },
  "dependencies": {
    "fs-extra": "^9.0.1"
  },
  "devDependencies": {
    "@babel/core": "^7.12.10",
    "@babel/cli": "^7.12.10",
    "@babel/preset-env": "^7.12.11",
    "babel-preset-minify": "^0.5.1"
  },
  "browserslist": [
    "defaults",
    "not IE 11",
    "maintained node versions"
  ]
}
